name: AppClaw Flows

on:
  workflow_dispatch:
    inputs:
      flow:
        description: 'Flow file to run (relative to flows/, leave empty to run all)'
        type: string
        default: ''
  push:
    paths:
      - 'flows/**'

jobs:
  appclaw:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4   # version comes from package.json "packageManager"

      - uses: actions/setup-node@v4
        with:
          # appclaw's deps (ai-sdk-ollama, pipenet) require node >=22.
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # The unscoped `appclaw` package is deprecated (its df-vision dep was
      # unpublished); the CLI lives on as the scoped @appclaw/cli 2.x.
      - name: Install AppClaw CLI
        run: npm install -g @appclaw/cli && appclaw --version

      # apps/*.apk is git-ignored — fetch the WebdriverIO demo app for the run.
      - name: Download demo app
        run: |
          mkdir -p apps
          curl -L -o apps/demo.apk \
            "$(curl -s https://api.github.com/repos/webdriverio/native-demo-app/releases/latest | grep -o 'https://[^"]*\.apk')"
          unzip -l apps/demo.apk >/dev/null && echo "✓ apps/demo.apk ready"

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Run AppClaw flows
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          arch: x86_64
          profile: pixel_6
          avd-name: appclaw_avd
          emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim
          disable-animations: true
          # Single-line invocation: the emulator-runner action mangles multi-line
          # shell (if/for) blocks, so the loop logic lives in scripts/run-flows.sh.
          # Empty input → run all flows/*. APP_PATH/DEVICE_UDID come from step env.
          script: bash scripts/run-flows.sh "${{ github.event.inputs.flow }}"
        env:
          # YAML flows are deterministic (zero LLM calls). These only apply if a
          # dom-agent fallback is invoked — pointed at a local Ollama model, no cloud key.
          LLM_PROVIDER: ollama
          LLM_BASE_URL: http://localhost:11434/v1
          LLM_MODEL: llama3.1
          APP_PATH: apps/demo.apk
          DEVICE_UDID: emulator-5554

      - name: Upload AppClaw artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: appclaw-results
          path: |
            appclaw-results/
            screen-*.png
          retention-days: 7
